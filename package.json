{
  "name": "mini-server-vanilla",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --define:process.env.NODE_ENV=\\\"production\\\" server.js --bundle --minify --platform=node --outfile=dist/app.js" 
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.16.6"
  }
}
